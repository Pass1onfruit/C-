双端队列
支持任意位置插入删除也支持随机访问
也就是说它既有vector的优点，也有list的优点
但是不能替代list和vector
缺点：
随机访问效率非常非常低，排序慢
迭代器遍历相对比较复杂，对效率也有一定影响

与vector比较，deque的优势是：
头部插入和删除时，不需要搬移元素，效率特别高
在扩容时，也不，需要搬移大量的元素，因此其效率是必vector高的
与list比较：
其底层是连续空间，空间利用率比较高，不需要存储额外字段

一个优化方案，可以用它来适配适配器
头尾插入删除效率还可以（都是o1），并不会用到随机访问

由 缓冲区buffer，中控器，迭代器组成

每个buffer申请时开的空间是一样大的
不增容，满了直接开辟一个新的小数组
通过中控映射管理多个小数组buffer
实现operator[]的随机访问：需要以计算访问的第i个数据在哪个buffer，所以当数据量大了以后，效率就变低了
迭代器比较复杂 四个指针
